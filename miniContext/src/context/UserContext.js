import React from 'react'

//crete the context
const UserContext = React.createContext()

export default UserContext;

{/* <UserContext>
    <login>
        <Card>
            <Data/> this component is attomatically accesed throw the context provider
        </Card>
    </login>
</UserContext> */}
